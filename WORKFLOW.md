# Ann&Lonny Website — Development Workflow

本文件說明 Lonny 與 Codex 在 Ann&Lonny 網站開發期間的合作方式。所有後續開發工作都應遵循以下規則。

## 1. 開始工作前

- 先閱讀 `PROJECT_SUMMARY.md`，了解目前功能、架構、訂課流程、已知問題與待辦事項。
- 檢查目前程式碼與 Git 狀態，不假設先前對話中提到的修改已經存在。
- 在修改程式碼前，先向 Lonny 簡短說明預計修改的內容、影響範圍與建議做法。
- 如果需求不清楚、可能有多種做法，或不同選擇會明顯影響網站結果，應先詢問 Lonny，不自行猜測。

## 2. 修改原則

- 除非 Lonny 明確要求，否則不得移除、隱藏或替換任何現有功能。
- 保留目前的設計語言，包括顏色、字體階層、留白、圖片呈現、卡片樣式、按鈕層級與 responsive 行為。
- 優先延續現有架構與內容管理方式，不為小型需求加入不必要的框架、套件或複雜功能。
- 保留既有的 OpenAI Sites、Vite、vinext、Cloudflare 與 Vercel 設定；只有在修改確實必要時才調整，並事先說明原因。
- 修改文字、課程、教練、FAQ、聯絡方式與雙語內容時，優先集中在 `app/content.ts` 維護。
- 中文與英文內容、SEO metadata、圖片替代文字及相關政策應保持一致。
- 不修改與當前任務無關的使用者檔案或既有變更。

## 3. 驗證方式

- 依修改範圍執行適當的 lint、typecheck、test 或 production build。
- 視覺修改應檢查手機、平板與桌面版面，以及中英文內容。
- 互動修改應檢查導覽、語言切換、訂課流程切換、FAQ 與聯絡連結。
- 圖片修改應確認檔案存在、格式適合網站、裁切正確、alt text 完整，且不造成不必要的載入負擔。
- 若驗證無法完成，應明確說明未檢查的項目與原因，不把未執行的檢查描述為已通過。

## 4. PROJECT_SUMMARY.md 維護

- 每完成一項功能、內容流程或架構變更後，都必須同步更新 `PROJECT_SUMMARY.md`。
- 更新內容應反映實際完成的結果，不記錄尚未實作的構想為現有功能。
- 新功能完成後，應從 Pending tasks 或 Future roadmap 移至 Current completed features。
- 檔案、技術、設計、訂課流程或已知問題有變更時，應同步更新對應章節。
- 每次更新時，重新填寫核對日期；若尚未建立 commit，應標示為 working tree changes，不得填入不存在的 commit hash。

## 5. CHANGELOG.md 維護

- 每完成一項程式或文件任務，都必須在 `CHANGELOG.md` 的當日日期下新增簡短紀錄。
- 依 `YYYY-MM-DD` 日期由新到舊排列，並使用 Added、Changed、Fixed、Documentation 等適合的分類。
- 記錄已完成的功能、修正、內容、設計、重構及文件變更，每項使用一句簡短條目。
- 若同一任務更新 `PROJECT_SUMMARY.md` 或其他文件，也必須把文件變更記入 `CHANGELOG.md`。
- 不記錄未實作的討論、失敗實驗，或未影響程式碼的暫時測試。

## 6. Git 與提交規則

- Commit 應保持小型、單一目的且容易審查。
- 不把彼此無關的功能、內容與重構混在同一個 commit。
- Commit 前先檢查變更範圍，確認沒有秘密資料、`.env`、本機快取、建置輸出或無關檔案。
- 未經 Lonny 指示，不執行 force push、歷史改寫、破壞性 reset，或覆蓋遠端內容。
- 未經 Lonny 同意，不自行推送至 GitHub 或部署正式網站。

## 7. 每項任務完成後

- 提供簡短摘要，說明修改了什麼。
- 列出主要變更檔案。
- 說明已完成的檢查及其結果。
- 確認 `PROJECT_SUMMARY.md` 與 `CHANGELOG.md` 已依實際變更同步更新。
- 清楚指出仍需 Lonny 確認、補充或決定的事項。
- 若建立 commit 或部署，提供對應 branch、commit hash 或網址。

## 8. 工作完成的定義

一項任務只有在以下條件都符合時才算完成：

1. 指定修改已實際完成。
2. 現有功能未被意外移除或破壞。
3. 已完成與風險相符的驗證。
4. `PROJECT_SUMMARY.md` 已同步更新。
5. `CHANGELOG.md` 已加入當日完成紀錄。
6. 已向 Lonny 提供簡短的完成摘要。
